public enum TokenType
{
    LEFT_PAREN,
    RIGHT_PAREN,

    PLUS,
    MINUS,
    STAR,
    SLASH,
    MOD,

    EQUAL,
    EQUAL_EQUAL,
    BANG,
    BANG_EQUAL,
    LESS,
    LESS_EQUAL,
    GREAT,
    GREAT_EQUAL,

    IDENTIFIER,
    STRING,
    NUMBER,

    VAR,
    PRINT,
    IF,
    ELSE,
    WHILE,
    TRUE,
    FALSE,
    NIL,

    EOF,

    SET,
    INITIALIZE,
    INPUT,
    OUTPUT,

    END,
    THEN,
    FOR,
    EACH,
    IN,
    TO,
    DO,
    REPEAT,
    UNTIL,
    CASE,
    OF,
    DEFAULT,

    CALL,
    RETURN,
    FUNCTION,
    PROCEDURE,

    AND,
    OR,
    NOT,
    XOR,

    DOT,
    COLON,
    COMMA
}